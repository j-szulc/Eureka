uid = root
gid = root
use chroot = no
max connections = 4

pid file = /var/run/rsyncd.pid
lock file = /var/run/rsync.lock
log file = /var/log/rsync.log

[Eureka]
    path = /notebooks/Eureka
    comment = Eureka
    read only = no
